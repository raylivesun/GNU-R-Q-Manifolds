#!/usr/bin/r

A <- c(10,20,30,40,50,60,70,80,90)
B <- c(10,20,30,40,50,60,70,80,90)
D <- c(10,20,30,40,50,60,70,80,90)
M <- c(10,20,30,40,50,60,70,80,90)

if (A || length(0L)) {
    print(A*M)
} else {
  return(A*M^2)
}

if (D || length(0L)) {
  print(D*B)
} else {
  return(D*B^2)
}


if (D || length(0L)) {
  print(D*A)
} else {
  return(D*A^2)
}

if (A || length(0L)) {
  print(A*M)
} else {
  return(A*M^2)
}

if (B || length(0L)) {
  print(B*M)
} else {
  return(B*M^2)
}


A <- c(100,200,300,400,500,600,700,800,900)
D <- c(100,200,300,400,500,600,700,800,900)
TB <- c(100,200,300,400,500,600,700,800,900)
TM <- c(100,200,300,400,500,600,700,800,900)
K <- c(100,200,300,400,500,600,700,800,900)


if (A || length(0L)) {
  print(A*TM)
} else {
  return(A*TM^2)
}

if (D || length(0L)) {
  print(D*TB)
} else {
  return(D*TB^2)
}


if (TB || length(0L)) {
  print(TB*TM)
} else {
  return(TB*TM^2)
}

if (TM || length(0L)) {
  print(TM*TB)
} else {
  return(TM*TB^2)
}

# leads bundle
D*A;K*M
A*M;D*K
D*B;K*M
B*M;D*K
# staticcal algebroids folds
Q1 <- factor(A*D)
Q2 <- factor(D*A)
Q3 <- factor(A*M)
Q4 <- factor(M*A)
Q5 <- factor(B*M)
Q6 <- factor(M*B)
Q7 <- factor(K*M)
Q8 <- factor(M*K)
Q9 <- factor(D*K)
Q10 <- factor(K*D)

Au <- function(Q) {
  Qi <- function(iu) {
    iu <- c(100,200,300,400,500,600,700,800,900);iu
    if (Q || length(0L)) {
        print(iu*2)
    } else {
      return(iu*2)
    }
    
  }
}

iuv <- function(u=-1) {
  if (u || length(0L)) {
    Iu <- c(100,200,300,400,500,600,700,800,900);Iu
    Qi <- factor(Iu);Qi
    Iv <- factor(Iu);Iv
  
  } else {
    return(Iu*Qi/Iv)
  }
}

f <- function(cm) {
  if (cm || length(0L)) {
      cm <- unique(cm, incomparables = FALSE);
      print(cm^2)
  } else {
    return(cm)
  }
}

uv <- function(cm, e) {
  if (cm || length(0L)) {
      cm <- unique(cm, incomparables = FALSE);
      print(cm^2)
  } else {
    return(cm)
  }
  if (e || length(0L)) {
      e <- numeric(length = 0L)
      print(e^2)
  } else {
    return(e)
  }
}

i <- function(cm, e, vect) {
  if (cm || length(0L)) {
    cm <- unique(cm, incomparables = FALSE);
    print(cm^2)
  } else {
    return(cm)
  }
  if (e || length(0L)) {
    e <- numeric(length = 0L)
    print(e^2)
  } else {
    return(e)
  }
  
  if (vect || length(0L)) {
    vect <- vector(mode = "logical", length = 0L)
    print(vect^2)
  } else {
    return(vect)
  }
  
}

u <- function(cm, e) {
  if (cm || length(0L)) {
    cm <- unique(cm, incomparables = FALSE);
    print(cm^2)
  } else {
    return(cm)
  }
  if (e || length(0L)) {
    e <- numeric(length = 0L)
    print(e^2)
  } else {
    return(e)
  }
}

Q2 = 0
# properties coordinates
E <- file.create("Q2.R", showWarnings = TRUE)
Q <- file.create("Q3.R", showWarnings = TRUE);file.create("Q4.R", showWarnings = TRUE)
Xa <- factor(M)
Ui <- factor(Q2)
Ei <- factor(Q2)
Xa <- function(x) {
  if (x || length(0L)) {
      print(x*pi)
  } else {
    return(x)
  }
}

ui <- function(iu, ti, i, x) {
  if (iu || length(0L)) {
      iu <- data.frame(c(10,20,30,40,50,60,70,80,90),c(100,200,300,400,500,600,700,800,900),
                       c(1000,2000,3000,4000,5000,6000,7000,8000,9000))
      ti <- vector(mode = "logical", length = 0L)
      i <- length(0L)
      x <- iu^pi
  } else {
    return(iu,ti,i,x)
  }
}

ei <- function(iu, ti, i, x) {
  if (iu || length(0L)) {
    iu <- data.frame(c(10,20,30,40,50,60,70,80,90),c(100,200,300,400,500,600,700,800,900),
                     c(1000,2000,3000,4000,5000,6000,7000,8000,9000))
    ti <- vector(mode = "logical", length = 0L)
    i <- length(0L)
    x <- iu^pi
  } else {
    return(iu,ti,i,x)
  }
}


i <- function(cm, e, vect) {
  if (cm || length(0L)) {
    cm <- unique(cm, incomparables = FALSE);
    print(cm^2)
  } else {
    return(cm)
  }
  if (e || length(0L)) {
    e <- numeric(length = 0L)
    print(e^2)
  } else {
    return(e)
  }
  
  if (vect || length(0L)) {
    vect <- vector(mode = "logical", length = 0L)
    print(vect^2)
  } else {
    return(vect)
  }
  
}

ui <- function(u = -1) {
  if (u || length(0L)) {
      u <- array(data = u, dim = length(u), dimnames = NULL)
      print(u*2)
  } else {
    return(u)
  }
}

Q <- function(x) {
   if (x || length(0L)) {
       six <- seq(x)
       print(six^6)
   } else {
     return(x)
   }
}

ei <- function(x) {
  if (x || length(0L)) {
    six <- seq(x)
    print(six^6)
  } else {
    return(x)
  }
}

Qai <- function(x) {
  if (x || length(0L)) {
    six <- seq(x)
    print(six^6)
  } else {
    return(x)
  }
}

a <- function(u = -1) {
    if (u || length(0L)) {
        ui <- seq(u)
        qai <- seq(u)
        print(ui/qai)
    } else {
      return(u)
    }
}

uv <- function(ui, qai, savk = -1) {
  if (ui || length(0L)) {
      u <- seq_len(ui)
      print(ui^2)
  } else {
    return(ui)
  }
  
  if (qai || length(0L)) {
      qai <- vector(mode = "logical", length = 0L)
      print(qai)
  } else {
    return(qai)
  }
  
  if (savk || length(0L)) {
      savk <- seq(savk)
      print(savk*2)
  } else {
    return(savk)
  }
}

ei <- function(j = -1, qkij) {
  if (j || length(0L)) {
      j <- seq(j)
      print(j^2)
  } else {
    return(j)
  }
  if (qkij || is.array(qkij)) {
      qkij <- data.frame(10,20,30,40,50)
      print(qkij^4)
  } else {
    return(qkij)
  }
}

Xa = 0
Xb = 0

ui <- function(qai, x) {
  if (qai || is.call(qai)) {
      qai <- lengths(qai, use.names = TRUE)
      print(qai^x)
  } else {
    return(qai)
  }
  if (x || is.expression(x)) {
      x <- is.call(x)
      print(x*pi)
  } else {
    return(x)
  }
}

Xa <- function(qai, x) {
  if (qai || is.call(qai)) {
    qai <- lengths(qai, use.names = TRUE)
    print(qai^x)
  } else {
    return(qai)
  }
  if (x || is.expression(x)) {
    x <- is.call(x)
    print(x*pi)
  } else {
    return(x)
  }
}

ui <- function(j = -1, qkij, x) {
  if (j || is.double(j)) {
      j <- vector(mode = "logical", length = 0L)
      print(j*0)
  } else {
    return(j)
  }
  if (qkij || is.environment(qkij)) {
      qkij <- tolower(qkij)
      print(qkij)
  } else {
    return(qkij)
  }
  
  if (x || is.atomic(x)) {
      x <- seq(x)
      print(x^2*4)
  } else {
    return(x)
  }
}

uj <- function(j = -1, qkij, x) {
  if (j || is.double(j)) {
    j <- vector(mode = "logical", length = 0L)
    print(j*0)
  } else {
    return(j)
  }
  if (qkij || is.environment(qkij)) {
    qkij <- tolower(qkij)
    print(qkij)
  } else {
    return(qkij)
  }
  
  if (x || is.atomic(x)) {
    x <- seq(x)
    print(x^2*4)
  } else {
    return(x)
  }
}

Xa = 0
Xb = 0


ei <- function(qai, x) {
  if (qai || is.data.frame(qai)) {
      qai <- for (qai in seq(qai)) {
        print(qai*2^6)
      }
  } else {
    return(qai)
  }
  
  if (x || is.call(x)) {
      x <- boot::calcium
      print(x*x)
  } else {
    return(x)
  }
}

xa <- function(qai, x) {
  if (qai || is.data.frame(qai)) {
    qai <- for (qai in seq(qai)) {
      print(qai*2^6)
    }
  } else {
    return(qai)
  }
  
  if (x || is.call(x)) {
    x <- boot::calcium
    print(x*x)
  } else {
    return(x)
  }
}


ei <- function(j = -1, qkij, x) {
  if (j || is.data.frame(qai)) {
    j <- for (j in seq(j)) {
      print(j*2^6)
    }
  } else {
    return(j)
  }
  
  if (qkij || is.call(qkij)) {
    qkij <- boot::calcium
    print(qkij*qkij)
  } else {
    return(qkij)
  }
  
  if (x || is.factor(x)) {
      x <- length(x)
      print(x)
  } else {
    return(x)
  }
}

ej <- function(j = -1, qkij, x) {
  if (j || is.data.frame(qai)) {
    j <- for (j in seq(j)) {
      print(j*2^6)
    }
  } else {
    return(j)
  }
  
  if (qkij || is.call(qkij)) {
    qkij <- boot::calcium
    print(qkij*qkij)
  } else {
    return(qkij)
  }
  
  if (x || is.factor(x)) {
    x <- length(x)
    print(x)
  } else {
    return(x)
  }
}

II1 <- prod(A)
II2 <- prod(B)

if (II1 || II2) {
    print(II1*II2)
} else {
  return(II1,II2)
}

IIa <- D
IIb <- D
IIa <- M
IIb <- M
# dialog Ta about D
X <- D
Xa = Xa;X
Ui = ui(j = -1, qkij = 10, x = 4)
Wa = ei(j = -1, qkij = 10, x = 4)
Zu = Au(10)
Ui = 0
Wa = 0
K = vector(mode = "logical", length = 0L)
Zu = K
K = M
w1 = ui(j = -1, qkij = 10, x = 4);ei(j= -1, qkij = 10, x = 4)
# close dialog ch
Xa = prod(D)
Ui = prod(D)
Na = prod(D)
U0 = prod(D)

# open window selection
Na = Wa + 1
A2 = 1 + 1 
U0 = Zu + 1; u(cm = 9, e = 5)
Ui = ui(j = -1, qkij = 10, x = 4)
Na = ei(j = -1, qkij = 10, x = 4)
U0 = ej(j = -1, qkij = 10, x = 4)
Na = prod(A)
U0 = prod(B)
# push soft to outer side local
IIb.D = prod(B)
IIa = prod(M)

# push pop link table
Xa = IIb.D
Ei = IIb.D
Wa = IIb.D

# vector perfect
Xa = IIb;IIa;IIb.D
Ei = IIb;IIa;IIb.D
Na = IIb;IIa;IIb.D
Tu = IIb;IIa;IIb.D

# a cool clear static perfume home
IIb;IIb.D = IIb
IIa = M


# popup files selector pop cups
Xa = IIb;IIa;IIb.D
Ei = IIb;IIa;IIb.D
Na = IIb;IIa;IIb.D
Tu = IIb;IIa;IIb.D
Su = IIb;IIa;IIb.D

# logic fireworks popup
M0 = IIb;IIa;IIb.D
Eu = IIb;IIa;IIb.D
Tu = IIb;IIa;IIb.D
Su = IIb;IIa;IIb.D
Zu = IIb;IIa;IIb.D
Wa = IIb;IIa;IIb.D
Na = IIb;IIa;IIb.D

# dove man care perfume
IIw = IIb;IIa;IIb.D
IIz = IIb;IIa;IIb.D

# miracle believe magic body
I12 = IIb;IIa;IIb.D

# miracle following magic body believe lives
Qx2.A2 <- function(x2, a2, x1) {
  if (x2 || length(0L)) {
      x2 <- numeric(length = 0L)
      print(x2)
  } else {
    return(x2)
  }
  if (a2 || length(0L)) {
      a2 <- is.array(a2)
      print(a2)
  } else {
    return(a2)
  }
  if (x1 || length(0L)) {
      x1 <- is.call(x1)
      print(x1)
  } else {
    return(x1)
  }
}

Qu2.i2 <- function(u1, i1, qi1, qi2, x1) {
  if (u1 != i1) {
      u1 <- typeof(u1)
      i1 <- typeof(i1)
      print(u1,i1)
  } else {
    return(u1+i1)
  }
  if (qi1 != qi2) {
      qi1 <- is.array(qi1)
      qi2 <- is.array(qi2)
      print(qi1,qi2)
  } else {
    return(qi1+qi2)
  }
  if (x1 || is.double(x1)) {
      x1 <- vector(mode = "logical", length = 0L)
      print(x1)
  } else {
    return(x1)
  }
}

Qwa.A2 <- function(w1, a1, qa1, qa2, x1) {
  if (w1 == a1) {
      w1 <- is.double(w1)
      a1 <- is.double(a1)
      print(w1+a1*2)
  } else {
    return(w1+a1*2)
  }
  if (qa1 == qa2) {
      qa1 <- is.factor(qa1)
      qa2 <- is.factor(qa2)
      print(qa1+qa2*2)
  } else {
    return(qa1+qa2*2)
  }
  if (x1 || length(0L)) {
      x1 <- vector(mode = "logical", length = 0L)
      print(x1)
  } else {
    return(x1)
  }
}

Qz2.u2 <- function(z1, u1, qu1, qu2, x1) {
  if (z1 != u1) {
      z1 <- is.expression(z1)
      u1 <- is.expression(u1)
      print(z1+u1)
  } else {
    return(z1+u1)
  }
  if (qu1 != qu2) {
      qu1 <- is.finite(qu1)
      qu2 <- is.finite(qu2)
      print(qu1+qu2)
  } else {
    return(qu1+qu2)
  }
  
  if (x1 || is.finite(x1)) {
      x1 <- seq(x1)
      print(x1*pi)
  } else {
    return(x1)
  }
}

Qe2.i2 <- function(e1, ei2, qi1, qi2, x1) {
  if (e1 || is.array(e1)) {
      e1 <- vector(mode = "logical", length = 0L)
      print(e1)
  } else {
    return(e1)
  }
  if (ei2 || is.call(ei2)) {
      ei2 <- seq(ei2)
      print(ei2)
  } else {
    return(ei2)
  }
  if (qi1 || is.complex(qi1)) {
      qi1 <- head(qi1)
      print(qi1)
  } else {
    return(qi1)
  }
  if (qi2 || is.array(qi2)) {
      qi1 <- seq(qi2)
      print(qi2)
  } else {
    return(qi2)
  }
  
  if (x1 == 90) {
      x1 <- is.character(x1)
      print(x1)
  } else {
    return(x1)
  }
}


Qn2.a2 <- function(n1, a2, qa1, qa2, x1) {
  if (n1 != length(0L)) {
      n1 <- c(n1, a2, qa1, qa2)
      print(n1)
  } else {
    return(n1)
  }
  if (a2 || is.complex(a2)) {
      a2 <- var(a2, y = NULL, na.rm = FALSE)
      print(a2*6)
  } else {
    return(a2)
  }
  if (qa1 || is.expression(qa1)) {
      qa1 <- force(qa1)
      print(qa1)
  } else {
    return(qa1)
  }
  
  if (qa2 || is.complex(qa2)) {
      qa2 <- solve(qa2, qa1)
      print(qa2)
  } else {
    return(qa2)
  }
  
  if (x1 || is.complex(x1)) {
      x1 <- is.call(x1)
      print(x1)
  } else {
    return(x1)
  }
}


Qs2.u2 <- function(n1, a2, qa1, qa2, x1 = -1) {
  if (n1 != length(0L)) {
    n1 <- c(n1, a2, qa1, qa2)
    print(n1)
  } else {
    return(n1)
  }
  if (a2 || is.complex(a2)) {
    a2 <- var(a2, y = NULL, na.rm = FALSE)
    print(a2*6)
  } else {
    return(a2)
  }
  if (qa1 || is.expression(qa1)) {
    qa1 <- force(qa1)
    print(qa1)
  } else {
    return(qa1)
  }
  
  if (qa2 || is.complex(qa2)) {
    qa2 <- solve(qa2, qa1)
    print(qa2)
  } else {
    return(qa2)
  }
  
  if (x1 || is.complex(x1)) {
    x1 <- is.call(x1)
    print(x1)
  } else {
    return(x1)
  }
}

# is a double Lie anti-algebraic if the manifold H is endowed with two homo-logical 
# vector fields Q1 and Q2 of weights (1, 0) and (0, 1), respectively, such that

Q1 = c(1,0)
Q2 = c(0,1)

# coordinating values
w1 = c(-1, 0)
w2 = c(0. -1)

# check move
Qr = c(1,0,0,0,0)
r = c(0,0,0,0,1)

T = prod(5)
T*prod(M)

# direct the point stress
T = prod(2*D)
T = prod(2*B)
T = prod(A)
T = prod(M)

Ei =51
Qai = 519
S2 = 22
S2xa = 2251
A = 1
B = 2
Ej = 510
Qkji = 225109
Sek = 2255
Ei.na = 5111
Qbai = 22219
Tu.qbu = 2922.22212
Snb = 2212
Na = 11
Qya.ji = 2291.109
Tu.qyui = 21922.229229
Sty = 22199
# clear information
Q1 = Ei+Qai+S2/S2xa+A/B+Ei+Ej+Qkji+S2/Sek+
     c(Ei.na+Qbai+Tu.qbu)^S2/Snb+c(A/B+Ei+Ej+Na+Qya.ji+Ei+Tu.qyui)*S2/Sty

Q2 = Ei+Qai+S2/S2xa+A/B+Ei+Ej+Qkji+S2/Sek+
   c(Ei.na+Qbai+Tu.qbu)^S2/Snb+c(A/B+Ei+Ej+Na+Qya.ji+Ei+Tu.qyui)*S2/Sty

Q3 = Ei+Qai+S2/S2xa+A/B+Ei+Ej+Qkji+S2/Sek+
  c(Ei.na+Qbai+Tu.qbu)^S2/Snb+c(A/B+Ei+Ej+Na+Qya.ji+Ei+Tu.qyui)*S2/Sty

Q4 = Ei+Qai+S2/S2xa+A/B+Ei+Ej+Qkji+S2/Sek+
  c(Ei.na+Qbai+Tu.qbu)^S2/Snb+c(A/B+Ei+Ej+Na+Qya.ji+Ei+Tu.qyui)*S2/Sty

Q5 = Ei+Qai+S2/S2xa+A/B+Ei+Ej+Qkji+S2/Sek+
  c(Ei.na+Qbai+Tu.qbu)^S2/Snb+c(A/B+Ei+Ej+Na+Qya.ji+Ei+Tu.qyui)*S2/Sty


# (we have regrouped terms here). Similarly, Q2 admits the vertical parity 
# reversion, which gives a vector field on ΠA D with fiber coordinates ui , 
# η α , θµ :

Qaa = 2211
Sxa = 22511
An = 11
Aui = 1229
Qjia = 221091
U0 = 220
Qju = 221025 
Suj = 222510
Nb = 12
Qyba = 22921
Sny = 2219
Ui = 229
Au0 = 1220
Sy0 = 2290

# declare affirmation
QII2 = Na+Qaa+S2/Sxa+c(c(-1)^An+Aui+Qjia-U0+Qju)^S2/Suj+Qyba+
       A/B+Na+Nb+S2/Sny+c(-c(-1)^A+B+A/B+Ui+Qyba+c(-1)^An+Au0+Qyba)^S2/Sy0

Q10 <- function(ei, qai, s2, a, b, qkij, sek) {
  if (ei || is.array(ei)) {
    ei <- match(ei, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(ei)
  } else {
    return(ei)
  }
  if (qai || is.array(qai)) {
      qai <- vector(mode = "logical", length = 0L) 
  } else {
    return(qai)
  }
  if (s2 || is.array(s2)) {
    s2 <- var(s2, y = NULL, na.rm = FALSE)
    print(s2)
  } else {
    return(s2)
  }
  if (a || is.atomic(a)) {
    a <- varimax(a, normalize = TRUE, eps = 1e-05)
    print(a^2)
  } else {
    return(a)
  }
  
  if (b || is.atomic(b)) {
    b <- varimax(b, normalize = TRUE, eps = 1e-05)
    print(b^2)
  } else {
    return(b)
  }
  
  if (qkij || is.array(qkij)) {
    qkij <- match(qkij, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(qkij)
  } else {
    return(qkij)
  }
  if (sek || is.array(sek)) {
    sek <- vector(mode = "logical", length = 0L)
  } else {
    return(sek)
  }
}

Q20 <- function(ei, qai, s2, a, b, qkij, sek) {
  if (ei || is.array(ei)) {
    ei <- match(ei, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(ei)
  } else {
    return(ei)
  }
  if (qai || is.array(qai)) {
    qai <- density(qai)
  } else {
    return(qai)
  }
  if (s2 || is.array(s2)) {
    s2 <- var(s2, y = NULL, na.rm = FALSE)
    print(s2)
  } else {
    return(s2)
  }
  if (a || is.atomic(a)) {
    a <- varimax(a, normalize = TRUE, eps = 1e-05)
    print(a^2)
  } else {
    return(a)
  }
  
  if (b || is.atomic(b)) {
    b <- varimax(b, normalize = TRUE, eps = 1e-05)
    print(b^2)
  } else {
    return(b)
  }
  
  if (qkij || is.array(qkij)) {
    qkij <- match(qkij, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(qkij)
  } else {
    return(qkij)
  }
  if (sek || is.array(sek)) {
    sek <- density(sek)
  } else {
    return(sek)
  }
}

Q30 <- function(ei, qai, s2, a, b, qkij, sek) {
  if (ei || is.array(ei)) {
    ei <- match(ei, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(ei)
  } else {
    return(ei)
  }
  if (qai || is.array(qai)) {
    qai <- density(qai)
  } else {
    return(qai)
  }
  if (s2 || is.array(s2)) {
    s2 <- var(s2, y = NULL, na.rm = FALSE)
    print(s2)
  } else {
    return(s2)
  }
  if (a || is.atomic(a)) {
    a <- varimax(a, normalize = TRUE, eps = 1e-05)
    print(a^2)
  } else {
    return(a)
  }
  
  if (b || is.atomic(b)) {
    b <- varimax(b, normalize = TRUE, eps = 1e-05)
    print(b^2)
  } else {
    return(b)
  }
  
  if (qkij || is.array(qkij)) {
    qkij <- match(qkij, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(qkij)
  } else {
    return(qkij)
  }
  if (sek || is.array(sek)) {
    sek <- density(sek)
  } else {
    return(sek)
  }
}

Q40 <- function(ei, qai, s2, a, b, qkij, sek) {
  if (ei || is.array(ei)) {
    ei <- match(ei, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(ei)
  } else {
    return(ei)
  }
  if (qai || is.array(qai)) {
    qai <- density(qai)
  } else {
    return(qai)
  }
  if (s2 || is.array(s2)) {
    s2 <- var(s2, y = NULL, na.rm = FALSE)
    print(s2)
  } else {
    return(s2)
  }
  if (a || is.atomic(a)) {
    a <- varimax(a, normalize = TRUE, eps = 1e-05)
    print(a^2)
  } else {
    return(a)
  }
  
  if (b || is.atomic(b)) {
    b <- varimax(b, normalize = TRUE, eps = 1e-05)
    print(b^2)
  } else {
    return(b)
  }
  
  if (qkij || is.array(qkij)) {
    qkij <- match(qkij, table = 5.4, nomatch = NA_integer_, incomparables = NULL)
    print(qkij)
  } else {
    return(qkij)
  }
  if (sek || is.array(sek)) {
    sek <- density(sek)
  } else {
    return(sek)
  }
}
