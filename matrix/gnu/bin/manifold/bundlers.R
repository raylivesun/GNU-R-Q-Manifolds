#!/usr/bin/r

E1 <- function(m1) {
  if (m1 != is.array(m1)) {
    m1 <- sin(m1)
    print(m1*2^5)
  } else {
    return(m1)
  }
}

E2 <- function(m2) {
  if (m2 != is.array(m2)) {
    m2 <- sin(m2)
    print(m2*2^5)
  } else {
    return(m2)
  }
}

